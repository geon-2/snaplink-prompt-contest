# 환경 변수 템플릿 — .env.local 로 복사 후 값 입력

# API 서버 주소 (vite 프록시 타겟 - 로컬 개발용)
API_TARGET=http://localhost:8000

# API 서버 직접 URL (프로덕션용, VITE_ 접두사로 클라이언트 코드에 주입)
# 로컬에서는 비워두면 vite 프록시 사용
VITE_API_BASE_URL=

# AWS S3
S3_BUCKET=
AWS_REGION=ap-northeast-2
